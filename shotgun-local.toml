# shotgun configuration for local process execution

[shotgun]
max_parallel = 2
test_timeout_secs = 60
retry_count = 3
stream_output = true

[provider]
type = "local"
shell = "/bin/sh"

[groups.unit]
type = "pytest"
paths = ["examples/tests"]
# markers = "not slow"
python = "python3"
extra_args = ["-s"]  # -s disables output capture so we see prints

# [groups.slow]
# type = "pytest"
# paths = ["examples/tests"]
# markers = "slow"
# python = "python3"
# extra_args = ["-s"]  # -s disables output capture so we see prints


[report]
output_dir = "test-results"
junit = true
junit_file = "junit.xml"
