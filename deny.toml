# Generated by rust-bucket v0.5.0. DO NOT EDIT BY HAND.

# Configuration for cargo-deny (https://github.com/EmbarkStudios/cargo-deny)
# This file defines security, license, and dependency policies for this crate.

[advisories]
# Check for security vulnerabilities in dependencies
# As of cargo-deny 0.16+, all advisories are denied by default
# Use `ignore` to allow specific advisories if needed
ignore = [
    # number_prefix is unmaintained but it's a transitive dep via indicatif
    # with no available upgrade path. Not a security issue.
    "RUSTSEC-2025-0119",
]

[licenses]
# Allow only approved permissive licenses
# As of cargo-deny 0.16+, unlicensed crates are denied by default
allow = [
    "MIT",
    "Apache-2.0",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "MPL-2.0",
    "Zlib",
    "Unlicense",
    "Unicode-3.0",
    "LicenseRef-Proprietary",
]
confidence-threshold = 0.8

# The workspace crate uses a proprietary license
[[licenses.clarify]]
crate = "offload"
expression = "LicenseRef-Proprietary"
license-files = [{ path = "LICENSE", hash = 0x12d82227 }]

[bans]
# Ban specific crates or versions
multiple-versions = "warn"
wildcards = "allow"
highlight = "all"
deny = []
skip = []
skip-tree = []

[sources]
# Only allow dependencies from crates.io
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
allow-git = []
