# Pytest configuration file to support running the example test suite.

# Note that Pytest detects "pytest root" based on the location of this file, so it's best to colocate this file with
# your szg.toml

[pytest]
# Create a marker for slow tests
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
